/**
 * RealtimePort: server-to-client push for order status. SSE is the MVP
 * implementation; a WebSocket adapter behind the same interface is what
 * driver location streaming and customer/driver chat will need later.
 */

export {};
