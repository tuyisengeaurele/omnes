/**
 * SmsPort: the interface every SMS gateway adapter implements. The mock
 * implementation logs instead of sending, but is otherwise wired the same
 * way a real provider would be.
 */

export {};
